#include <iostream>
#include <stack>
#include <string>

using namespace std;

int main()
{
	stack<char> str;
	//str.pop();
	if (str.empty())
	{
		cout << "empty";
		//if (str.top() != 3)
		{
			cout << "empty";

			str.pop();
		}
	}
}