#include <iostream>
#include <queue>

using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	queue<int> Q;
	
	int N;
	cin >> N;

	for (int i = 0; i < N; i++)
	{
		int M, C;
		cin >> M >> C;

		for (int i = 0; i < M; i++)
		{

		}
	}
}